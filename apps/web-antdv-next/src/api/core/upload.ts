/** Placeholder until a governed upload endpoint is wired (Phase 7.6+). */
export async function upload_file(_options: Record<string, unknown>) {
  throw new Error('File upload API is not configured');
}
